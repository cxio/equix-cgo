# Equi-X v2 Cgo 封装 实现 Plan

> **给 agentic workers：** 必需子技能：使用 subagent-driven-development（推荐）或 executing-plans 逐任务实现此 plan。步骤使用复选框（`- [ ]`）语法进行跟踪。

**Goal:** 把 `tevador/equix` 的 `equix_v2` 分支封装为可 `go get` 的模块 `github.com/cxio/equix`，提供 Solve/Verify、nonce 变体、Hashes 往返与纯算术 `VerifyHashes`。

**Architecture:** 公开包 `equix` 不含 `import "C"`。`internal/native` 用 cgo 编译钉死的 `third_party/{equix,hashx,hashwx}` 快照（薄 stub `#include` 各上游 `.c`，另加本仓库 `hashes.c` 胶水）。分配 context 时始终带 `EQUIX_V2`，JIT `COMPILE` 失败则回退解释器。包级函数用 `sync.Pool` 复用 `Solver`/`Verifier`。

**Tech Stack:** Go 1.27.0、cgo、C11、官方 Equi-X v2 / HashX / HashWX（LGPL-3.0）。

## 全局约束

- 模块路径：`github.com/cxio/equix-cgo`
- Go 版本：`go 1.27.0`
- 算法：只走 Equi-X v2（`EQUIX_V2` + HashWX）；不导出 v1、HashX/HashWX、`COMPILE`、大页
- 上游 commit：equix `350a85dedda1344637dac09a1de786ee63a5fb01`（分支 `equix_v2`）；hashx `08babdf4f41b0b8991d1fa94914c7c6902de0cb6`；hashwx `d771cbf6cdc070755f7d137cdcf9d781af14da3f`
- 不修改 `third_party/` 内上游 C 源码
- 不提供 `CGO_ENABLED=0` 回退；Windows cgo 用 MinGW，不用 MSVC
- 宏：`HASHX_SIZE=8`、`HASHX_STATIC`、`HASHWX_STATIC`、`EQUIX_STATIC`；`-std=c11 -O2`；不加 `-march=native`
- nonce 布局：Equi-X 输入 = `challenge || little-endian uint64`
- 不使用 `EQUIX_CTX_HUGEPAGES`
- Go 代码 MIT；`third_party` 保留 LGPL-3.0

## 文件结构

| 路径 | 职责 |
|------|------|
| `errors.go` | 导出哨兵错误（含 `ErrClosed`） |
| `solution.go` | `Solution`、`Hashes`、`Result`、16 字节编解码、未导出 `appendNonce` |
| `hashes.go` | `VerifyHashes`（纯 Go） |
| `solver.go` | `Solver` 及其 Solve* 方法 |
| `verifier.go` | `Verifier` 及其 Verify* 方法 |
| `equix.go` | 包级函数 + 两个 `sync.Pool` |
| `equix_test.go` | 需要 C 的集成测试 |
| `hashes_test.go` | `VerifyHashes` 与 `Solution` 编解码（无 cgo） |
| `internal/native/cgo.go` | `#cgo` 指令与 `import "C"` |
| `internal/native/native.go` | alloc/free/solve/verify 的 Go 封装 |
| `internal/native/hashes.go` | 取哈希的 Go 封装 |
| `internal/native/hashes.c` | `hashwx_make` + `hashwx_exec` 胶水 |
| `internal/native/version.go` | 三个上游 SHA（触发 cgo 重编） |
| `internal/native/stub_*.c` | 每个上游翻译单元一个 `#include` |
| `third_party/equix/` `hashx/` `hashwx/` | 上游快照 + LICENSE |
| `README.md` | 用法、平台、双重许可、上游 commit |
| `go.mod` | 已有，保持 `module github.com/cxio/equix-cgo` / `go 1.27.0` |

`internal/native` 不得 import `github.com/cxio/equix-cgo`（循环）。native 的 Verify 返回与 C `equix_result` 相同的 `int`（0=OK, 1=CHALLENGE, 2=ORDER, 3=PARTIAL_SUM, 4=FINAL_SUM）；公开包映射到哨兵错误。

---

### Task 1: 纯 Go 类型、错误、VerifyHashes

**Files:**
- Create: `errors.go`
- Create: `solution.go`
- Create: `hashes.go`
- Create: `hashes_test.go`
- Modify: `go.mod`（若尚未纳入 git，一并加入；内容已是 `module github.com/cxio/equix-cgo` / `go 1.27.0`）

**Interfaces:**
- Consumes: 无
- Produces:
  - `var ErrNotSupported, ErrChallenge, ErrOrder, ErrPartialSum, ErrFinalSum, ErrClosed error`
  - `type Solution [8]uint16` 及 `MarshalBinary() ([]byte, error)` / `UnmarshalBinary([]byte) error`
  - `type Hashes [8]uint64`
  - `type Result struct { Solution Solution; Hashes Hashes }`
  - `func VerifyHashes(h Hashes) error`
  - `func appendNonce(challenge []byte, nonce uint64) []byte`（未导出）

- [ ] **Step 1: 编写失败的测试**

创建 `hashes_test.go`：

```go
package equix

import (
	"bytes"
	"encoding/binary"
	"errors"
	"testing"
)

func TestVerifyHashesAllZero(t *testing.T) {
	if err := VerifyHashes(Hashes{}); err != nil {
		t.Fatalf("zero hashes: %v", err)
	}
}

func TestVerifyHashesStage1(t *testing.T) {
	err := VerifyHashes(Hashes{1, 0, 0, 0, 0, 0, 0, 0})
	if !errors.Is(err, ErrPartialSum) {
		t.Fatalf("got %v, want ErrPartialSum", err)
	}
}

func TestVerifyHashesDoesNotReturnOrder(t *testing.T) {
	err := VerifyHashes(Hashes{1, 0, 0, 0, 0, 0, 0, 0})
	if errors.Is(err, ErrOrder) {
		t.Fatal("VerifyHashes must not return ErrOrder")
	}
}

func TestSolutionBinaryRoundTrip(t *testing.T) {
	var s Solution
	s[0], s[7] = 0x6c31, 0xe259
	b, err := s.MarshalBinary()
	if err != nil {
		t.Fatal(err)
	}
	if len(b) != 16 {
		t.Fatalf("len=%d", len(b))
	}
	if binary.LittleEndian.Uint16(b[0:2]) != 0x6c31 {
		t.Fatalf("idx0 encoding")
	}
	var s2 Solution
	if err := s2.UnmarshalBinary(b); err != nil {
		t.Fatal(err)
	}
	if s2 != s {
		t.Fatalf("%v != %v", s2, s)
	}
}

func TestSolutionUnmarshalRejectsLength(t *testing.T) {
	var s Solution
	if err := s.UnmarshalBinary([]byte{1, 2, 3}); err == nil {
		t.Fatal("want error")
	}
}

func TestAppendNonce(t *testing.T) {
	got := appendNonce([]byte{0xaa}, 0x0102030405060708)
	want := []byte{0xaa, 0x08, 0x07, 0x06, 0x05, 0x04, 0x03, 0x02, 0x01}
	if !bytes.Equal(got, want) {
		t.Fatalf("%x != %x", got, want)
	}
	if !bytes.Equal(appendNonce(nil, 0), make([]byte, 8)) {
		t.Fatal("nil challenge")
	}
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `go test ./ -run 'TestVerifyHashes|TestSolution|TestAppendNonce' -count=1`

Expected: 编译失败（`VerifyHashes` / `Solution` / `ErrPartialSum` 未定义）

- [ ] **Step 3: 编写最小实现**

`errors.go`:

```go
package equix

import "errors"

var (
	ErrNotSupported = errors.New("equix: requested context type not supported")
	ErrChallenge    = errors.New("equix: invalid challenge")
	ErrOrder        = errors.New("equix: indices not in required order")
	ErrPartialSum   = errors.New("equix: partial sum missing trailing zeros")
	ErrFinalSum     = errors.New("equix: hashes do not sum to zero")
	ErrClosed       = errors.New("equix: use of closed solver or verifier")
)
```

`solution.go`:

```go
package equix

import (
	"encoding/binary"
	"fmt"
)

type Solution [8]uint16

type Hashes [8]uint64

type Result struct {
	Solution Solution
	Hashes   Hashes
}

func (s Solution) MarshalBinary() ([]byte, error) {
	b := make([]byte, 16)
	for i := 0; i < 8; i++ {
		binary.LittleEndian.PutUint16(b[i*2:], s[i])
	}
	return b, nil
}

func (s *Solution) UnmarshalBinary(data []byte) error {
	if len(data) != 16 {
		return fmt.Errorf("equix: solution must be 16 bytes, got %d", len(data))
	}
	for i := 0; i < 8; i++ {
		s[i] = binary.LittleEndian.Uint16(data[i*2:])
	}
	return nil
}

func appendNonce(challenge []byte, nonce uint64) []byte {
	out := make([]byte, len(challenge)+8)
	copy(out, challenge)
	binary.LittleEndian.PutUint64(out[len(challenge):], nonce)
	return out
}
```

`hashes.go`（掩码与官方 `solver.h` 一致：15/30/60 位）：

```go
package equix

const (
	stage1Mask = uint64(1<<15) - 1
	stage2Mask = uint64(1<<30) - 1
	fullMask   = uint64(1<<60) - 1
)

func VerifyHashes(h Hashes) error {
	pair0 := h[0] + h[1]
	if pair0&stage1Mask != 0 {
		return ErrPartialSum
	}
	pair1 := h[2] + h[3]
	if pair1&stage1Mask != 0 {
		return ErrPartialSum
	}
	pair4 := pair0 + pair1
	if pair4&stage2Mask != 0 {
		return ErrPartialSum
	}
	pair2 := h[4] + h[5]
	if pair2&stage1Mask != 0 {
		return ErrPartialSum
	}
	pair3 := h[6] + h[7]
	if pair3&stage1Mask != 0 {
		return ErrPartialSum
	}
	pair5 := pair2 + pair3
	if pair5&stage2Mask != 0 {
		return ErrPartialSum
	}
	pair6 := pair4 + pair5
	if pair6&fullMask != 0 {
		return ErrFinalSum
	}
	return nil
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `go test ./ -run 'TestVerifyHashes|TestSolution|TestAppendNonce' -count=1`

Expected: `PASS`（无需 cgo）

- [ ] **Step 5: Commit**

```bash
git add go.mod errors.go solution.go hashes.go hashes_test.go
git commit -m "Add Solution/Hashes types and pure-Go VerifyHashes."
```

---

### Task 2: 上游快照、cgo stub、native alloc/free

**Files:**
- Create: `third_party/equix/` `third_party/hashx/` `third_party/hashwx/`（含各 LICENSE）
- Create: `internal/native/version.go`
- Create: `internal/native/cgo.go`
- Create: `internal/native/native.go`
- Create: `internal/native/stub_equix_context.c` `stub_equix_equix.c` `stub_equix_solver.c`
- Create: `internal/native/stub_hashx_blake2.c` `stub_hashx_compiler.c` `stub_hashx_compiler_a64.c` `stub_hashx_compiler_x86.c` `stub_hashx_context.c` `stub_hashx_hashx.c` `stub_hashx_program.c` `stub_hashx_program_exec.c` `stub_hashx_siphash.c` `stub_hashx_siphash_rng.c` `stub_hashx_virtual_memory.c`
- Create: `internal/native/stub_hashwx_compiler.c` `stub_hashwx_compiler_a64.c` `stub_hashwx_compiler_wasm.c` `stub_hashwx_compiler_x86.c` `stub_hashwx_context.c` `stub_hashwx_hashwx.c` `stub_hashwx_program.c` `stub_hashwx_program_exec.c` `stub_hashwx_siphash_rng.c` `stub_hashwx_virtual_memory.c`
- Create: `internal/native/native_test.go`

**Interfaces:**
- Consumes: 无公开包符号
- Produces:
  - `native.Context`（不透明，持有 `*C.equix_ctx`）
  - `func NewSolver() (*Context, error)`
  - `func NewVerifier() (*Context, error)`
  - `func (c *Context) Close()`
  - 常量 `EquixCommit` `HashxCommit` `HashwxCommit`

- [ ] **Step 1: 写入 third_party 快照**

需要网络。在仓库根执行（不要把 `.git` 拷进 third_party）：

```bash
set -euo pipefail
TMP=$(mktemp -d)
git clone https://github.com/tevador/equix.git "$TMP/equix"
git -C "$TMP/equix" checkout 350a85dedda1344637dac09a1de786ee63a5fb01
git -C "$TMP/equix" submodule update --init
git -C "$TMP/equix/hashx" checkout 08babdf4f41b0b8991d1fa94914c7c6902de0cb6
git -C "$TMP/equix/hashwx" checkout d771cbf6cdc070755f7d137cdcf9d781af14da3f

mkdir -p third_party/equix third_party/hashx third_party/hashwx
cp -R "$TMP/equix/include" "$TMP/equix/src" "$TMP/equix/LICENSE" "$TMP/equix/README.md" third_party/equix/
cp -R "$TMP/equix/hashx/include" "$TMP/equix/hashx/src" "$TMP/equix/hashx/LICENSE" "$TMP/equix/hashx/README.md" third_party/hashx/
cp -R "$TMP/equix/hashwx/include" "$TMP/equix/hashwx/src" "$TMP/equix/hashwx/LICENSE" "$TMP/equix/hashwx/README.md" third_party/hashwx/
rm -rf "$TMP"

# 确认 pin
test -f third_party/equix/include/equix.h
test -f third_party/hashx/include/hashx.h
test -f third_party/hashwx/include/hashwx.h
grep -q EQUIX_V2 third_party/equix/include/equix.h
```

不要 stub `tests.c` / `bench.c`。

- [ ] **Step 2: 编写失败的 native 测试**

`internal/native/native_test.go`：

```go
package native

import "testing"

func TestNewSolverAndClose(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.fatal(err)
	}
	s.Close()
	s.Close()
}

func TestNewVerifierAndClose(t *testing.T) {
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	v.Close()
}

func TestVersionPins(t *testing.T) {
	if EquixCommit != "350a85dedda1344637dac09a1de786ee63a5fb01" {
		t.Fatalf("equix pin")
	}
	if HashxCommit != "08babdf4f41b0b8991d1fa94914c7c6902de0cb6" {
		t.Fatalf("hashx pin")
	}
	if HashwxCommit != "d771cbf6cdc070755f7d137cdcf9d781af14da3f" {
		t.Fatalf("hashwx pin")
	}
}
```

- [ ] **Step 3: 运行测试验证失败**

Run: `CGO_ENABLED=1 go test ./internal/native -count=1`

Expected: 编译失败（`NewSolver` 未定义，或尚无 C 源）

- [ ] **Step 4: stub、cgo、alloc 实现**

每个 stub 文件只有一行 `#include`，路径相对 `internal/native/`。

equix 三份：

```c
#include "../../third_party/equix/src/context.c"
```

```c
#include "../../third_party/equix/src/equix.c"
```

```c
#include "../../third_party/equix/src/solver.c"
```

hashx（文件名 `stub_hashx_<name>.c`）`#include "../../third_party/hashx/src/<name>.c"`，`<name>` 为：`blake2` `compiler` `compiler_a64` `compiler_x86` `context` `hashx` `program` `program_exec` `siphash` `siphash_rng` `virtual_memory`。

hashwx（`stub_hashwx_<name>.c`）`#include "../../third_party/hashwx/src/<name>.c"`，`<name>` 为：`compiler` `compiler_a64` `compiler_wasm` `compiler_x86` `context` `hashwx` `program` `program_exec` `siphash_rng` `virtual_memory`。

`internal/native/version.go`：

```go
package native

const (
	EquixCommit  = "350a85dedda1344637dac09a1de786ee63a5fb01"
	HashxCommit  = "08babdf4f41b0b8991d1fa94914c7c6902de0cb6"
	HashwxCommit = "d771cbf6cdc070755f7d137cdcf9d781af14da3f"
)
```

`internal/native/cgo.go`：

```go
package native

/*
#cgo CFLAGS: -std=c11 -O2 -DHASHX_SIZE=8 -DHASHX_STATIC -DHASHWX_STATIC -DEQUIX_STATIC
#cgo CFLAGS: -I${SRCDIR}/../../third_party/equix/include
#cgo CFLAGS: -I${SRCDIR}/../../third_party/equix/src
#cgo CFLAGS: -I${SRCDIR}/../../third_party/hashx/include
#cgo CFLAGS: -I${SRCDIR}/../../third_party/hashx/src
#cgo CFLAGS: -I${SRCDIR}/../../third_party/hashwx/include
#cgo CFLAGS: -I${SRCDIR}/../../third_party/hashwx/src
#cgo windows LDFLAGS: -ladvapi32
#include <stdint.h>
#include <stdlib.h>
#include <equix.h>
*/
import "C"
```

`internal/native/native.go`：

```go
package native

import (
	"errors"
	"fmt"
	"unsafe"
)

var ErrNotSupported = errors.New("equix: requested context type not supported")

type Context struct {
	ptr *C.equix_ctx
}

func isNotSupp(p *C.equix_ctx) bool {
	return uintptr(unsafe.Pointer(p)) == ^uintptr(0)
}

func alloc(base C.equix_ctx_flags) (*Context, error) {
	v2 := C.equix_ctx_flags(C.EQUIX_V2)
	p := C.equix_alloc(base | v2 | C.EQUIX_CTX_COMPILE)
	if isNotSupp(p) {
		p = C.equix_alloc(base | v2)
	}
	if p == nil {
		return nil, errors.New("equix: out of memory")
	}
	if isNotSupp(p) {
		return nil, ErrNotSupported
	}
	return &Context{ptr: p}, nil
}

func NewSolver() (*Context, error) {
	return alloc(C.EQUIX_CTX_SOLVE)
}

func NewVerifier() (*Context, error) {
	return alloc(C.EQUIX_CTX_VERIFY)
}

func (c *Context) Close() {
	if c == nil || c.ptr == nil {
		return
	}
	C.equix_free(c.ptr)
	c.ptr = nil
}

func (c *Context) dead() error {
	if c == nil || c.ptr == nil {
		return errors.New("equix: nil context")
	}
	return nil
}

func cChallenge(ch []byte) (unsafe.Pointer, C.size_t, func()) {
	if len(ch) == 0 {
		return nil, 0, func() {}
	}
	p := C.CBytes(ch)
	return p, C.size_t(len(ch)), func() { C.free(p) }
}

func (c *Context) Solve(challenge []byte) ([][8]uint16, error) {
	if err := c.dead(); err != nil {
		return nil, err
	}
	var out [C.EQUIX_MAX_SOLS]C.equix_solution
	ptr, n, free := cChallenge(challenge)
	defer free()
	got := int(C.equix_solve(c.ptr, ptr, n, (*C.equix_solution)(unsafe.Pointer(&out[0]))))
	if got < 0 || got > int(C.EQUIX_MAX_SOLS) {
		return nil, fmt.Errorf("equix: invalid solution count %d", got)
	}
	sols := make([][8]uint16, got)
	for i := 0; i < got; i++ {
		for j := 0; j < 8; j++ {
			sols[i][j] = uint16(out[i].idx[j])
		}
	}
	return sols, nil
}

func (c *Context) Verify(challenge []byte, idx [8]uint16) (int, error) {
	if err := c.dead(); err != nil {
		return -1, err
	}
	var sol C.equix_solution
	for i := 0; i < 8; i++ {
		sol.idx[i] = C.equix_idx(idx[i])
	}
	ptr, n, free := cChallenge(challenge)
	defer free()
	code := int(C.equix_verify(c.ptr, ptr, n, &sol))
	return code, nil
}
```

`Solve`/`Verify` 在本任务测试中可以不调用；写出以免下一任务再拆 cgo 边界。若本步 `equix_solve` 链接失败，先修编译再继续。

- [ ] **Step 5: 运行测试验证通过**

Run: `CGO_ENABLED=1 go test ./internal/native -count=1`

Expected: `PASS`。若 `compiler_wasm.c` 在非 wasm 上报错，读该文件的 `#ifdef`；只在缺守卫时于 **stub** 外包 `#if defined(__wasm__)`（不要改 `third_party/`）。

- [ ] **Step 6: Commit**

```bash
git add third_party internal/native
git commit -m "Vendor Equi-X v2 C sources and add cgo solver/verifier alloc."
```

---

### Task 3: 公开 Solver / Verifier 与无 nonce Solve/Verify

**Files:**
- Create: `solver.go`
- Create: `verifier.go`
- Create: `map.go`（C 结果码 → 哨兵错误）
- Create: `equix_test.go`

**Interfaces:**
- Consumes: `native.NewSolver` `native.NewVerifier` `(*native.Context).Solve` `(*native.Context).Verify` `(*native.Context).Close`；`Solution`；`ErrClosed` 等
- Produces:
  - `func NewSolver() (*Solver, error)`
  - `func (*Solver) Solve(challenge []byte) ([]Solution, error)`
  - `func (*Solver) Close() error`
  - `func NewVerifier() (*Verifier, error)`
  - `func (*Verifier) Verify(challenge []byte, sol Solution) error`
  - `func (*Verifier) Close() error`
  - `func mapVerify(code int) error`

`mapVerify`：`0→nil`，`1→ErrChallenge`，`2→ErrOrder`，`3→ErrPartialSum`，`4→ErrFinalSum`，其它 `fmt.Errorf("equix: unknown result %d", code)`。

- [ ] **Step 1: 编写失败的测试**

`equix_test.go`（本文件此后各任务追加用例，本步先这些）：

```go
package equix

import (
	"errors"
	"testing"
)

func findSolution(t *testing.T) (challenge []byte, sol Solution) {
	t.Helper()
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	for n := 0; n < 32; n++ {
		ch := []byte{byte(n)}
		sols, err := s.Solve(ch)
		if err != nil {
			t.Fatal(err)
		}
		if len(sols) > 0 {
			return ch, sols[0]
		}
	}
	t.Fatal("no Equi-X v2 solution in 32 one-byte challenges")
	return nil, Solution{}
}

func TestSolveVerify(t *testing.T) {
	ch, sol := findSolution(t)
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	if err := v.Verify(ch, sol); err != nil {
		t.Fatal(err)
	}
}

func TestVerifyOrder(t *testing.T) {
	ch, sol := findSolution(t)
	sol[0], sol[1] = sol[1], sol[0]
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	if err := v.Verify(ch, sol); !errors.Is(err, ErrOrder) {
		t.Fatalf("got %v, want ErrOrder", err)
	}
}

func TestVerifyPartialSumSwap(t *testing.T) {
	ch, sol := findSolution(t)
	sol[1], sol[2] = sol[2], sol[1]
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	err = v.Verify(ch, sol)
	if !errors.Is(err, ErrPartialSum) && !errors.Is(err, ErrOrder) {
		t.Fatalf("got %v, want ErrPartialSum or ErrOrder", err)
	}
}

func TestCloseIdempotentAndUse(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	if err := s.Close(); err != nil {
		t.Fatal(err)
	}
	if err := s.Close(); err != nil {
		t.Fatal(err)
	}
	if _, err := s.Solve(nil); !errors.Is(err, ErrClosed) {
		t.Fatalf("got %v, want ErrClosed", err)
	}
	var n *Solver
	if err := n.Close(); err != nil {
		t.Fatal(err)
	}
}

func TestEmptyChallengeNoPanic(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	sols, err := s.Solve(nil)
	if err != nil {
		t.Fatal(err)
	}
	if len(sols) > 8 {
		t.Fatalf("len=%d", len(sols))
	}
	if sols == nil {
		t.Fatal("Solve must return empty slice, not nil")
	}
}
```

`toSolutions` 对 0 个解使用 `make([]Solution, 0)`，保证非 nil。

- [ ] **Step 2: 运行测试验证失败**

Run: `CGO_ENABLED=1 go test ./ -run 'TestSolveVerify|TestVerifyOrder|TestVerifyPartialSumSwap|TestCloseIdempotentAndUse|TestEmptyChallengeNoPanic' -count=1`

Expected: 失败（`NewSolver` 未定义）

- [ ] **Step 3: 编写最小实现**

`map.go`：

```go
package equix

import "fmt"

func mapVerify(code int) error {
	switch code {
	case 0:
		return nil
	case 1:
		return ErrChallenge
	case 2:
		return ErrOrder
	case 3:
		return ErrPartialSum
	case 4:
		return ErrFinalSum
	default:
		return fmt.Errorf("equix: unknown result %d", code)
	}
}

func toSolutions(in [][8]uint16) []Solution {
	out := make([]Solution, len(in))
	for i := range in {
		out[i] = Solution(in[i])
	}
	return out
}
```

`solver.go`：

```go
package equix

import (
	"errors"

	"github.com/cxio/equix-cgo/internal/native"
)

type Solver struct {
	ctx *native.Context
}

func NewSolver() (*Solver, error) {
	ctx, err := native.NewSolver()
	if err != nil {
		if errors.Is(err, native.ErrNotSupported) {
			return nil, ErrNotSupported
		}
		return nil, err
	}
	return &Solver{ctx: ctx}, nil
}

func (s *Solver) closed() error {
	if s == nil || s.ctx == nil {
		return ErrClosed
	}
	return nil
}

func (s *Solver) Solve(challenge []byte) ([]Solution, error) {
	if err := s.closed(); err != nil {
		return nil, err
	}
	raw, err := s.ctx.Solve(challenge)
	if err != nil {
		return nil, err
	}
	return toSolutions(raw), nil
}

func (s *Solver) Close() error {
	if s == nil || s.ctx == nil {
		return nil
	}
	s.ctx.Close()
	s.ctx = nil
	return nil
}
```

`verifier.go`：

```go
package equix

import (
	"errors"

	"github.com/cxio/equix-cgo/internal/native"
)

type Verifier struct {
	ctx *native.Context
}

func NewVerifier() (*Verifier, error) {
	ctx, err := native.NewVerifier()
	if err != nil {
		if errors.Is(err, native.ErrNotSupported) {
			return nil, ErrNotSupported
		}
		return nil, err
	}
	return &Verifier{ctx: ctx}, nil
}

func (v *Verifier) closed() error {
	if v == nil || v.ctx == nil {
		return ErrClosed
	}
	return nil
}

func (v *Verifier) Verify(challenge []byte, sol Solution) error {
	if err := v.closed(); err != nil {
		return err
	}
	code, err := v.ctx.Verify(challenge, [8]uint16(sol))
	if err != nil {
		return err
	}
	return mapVerify(code)
}

func (v *Verifier) Close() error {
	if v == nil || v.ctx == nil {
		return nil
	}
	v.ctx.Close()
	v.ctx = nil
	return nil
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `CGO_ENABLED=1 go test ./ -run 'TestSolveVerify|TestVerifyOrder|TestVerifyPartialSumSwap|TestCloseIdempotentAndUse|TestEmptyChallengeNoPanic' -count=1`

Expected: `PASS`（`TestSolveVerify` 可能要数秒，solver 约 1.8 MiB）

- [ ] **Step 5: Commit**

```bash
git add solver.go verifier.go map.go equix_test.go internal/native/native.go
git commit -m "Expose Solver and Verifier Solve/Verify over Equi-X v2."
```

---

### Task 4: nonce 变体

**Files:**
- Modify: `solver.go`
- Modify: `verifier.go`
- Modify: `equix_test.go`

**Interfaces:**
- Consumes: `appendNonce`；`(*Solver).Solve`；`(*Verifier).Verify`
- Produces:
  - `func (*Solver) SolveWithNonce(challenge []byte, nonce uint64) ([]Solution, error)`
  - `func (*Verifier) VerifyWithNonce(challenge []byte, nonce uint64, sol Solution) error`

- [ ] **Step 1: 编写失败的测试**

追加到 `equix_test.go`：

```go
func TestNonceRoundTrip(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	ch := []byte("cxio")
	var nonce uint64 = 7
	var sols []Solution
	for n := uint64(0); n < 32 && len(sols) == 0; n++ {
		nonce = n
		sols, err = s.SolveWithNonce(ch, nonce)
		if err != nil {
			t.Fatal(err)
		}
	}
	if len(sols) == 0 {
		t.Fatal("no solution")
	}
	if err := v.VerifyWithNonce(ch, nonce, sols[0]); err != nil {
		t.Fatal(err)
	}
	if err := v.VerifyWithNonce(ch, nonce+1, sols[0]); err == nil {
		t.Fatal("wrong nonce must fail")
	}
	if err := v.VerifyWithNonce([]byte("other"), nonce, sols[0]); err == nil {
		t.Fatal("wrong challenge must fail")
	}
}

func TestNonceConcatInvariant(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	ch := []byte{0x11, 0x22}
	var nonce uint64
	var sols []Solution
	for n := uint64(0); n < 32 && len(sols) == 0; n++ {
		nonce = n
		sols, err = s.SolveWithNonce(ch, nonce)
		if err != nil {
			t.Fatal(err)
		}
	}
	if len(sols) == 0 {
		t.Fatal("no solution")
	}
	wired := appendNonce(ch, nonce)
	if err := v.Verify(wired, sols[0]); err != nil {
		t.Fatal(err)
	}
	wiredSols, err := s.Solve(wired)
	if err != nil {
		t.Fatal(err)
	}
	if len(wiredSols) != len(sols) {
		t.Fatalf("len %d != %d", len(wiredSols), len(sols))
	}
	for i := range sols {
		if wiredSols[i] != sols[i] {
			t.Fatalf("solution %d diverged", i)
		}
	}
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `CGO_ENABLED=1 go test ./ -run 'TestNonceRoundTrip|TestNonceConcatInvariant' -count=1`

Expected: 失败（方法未定义）

- [ ] **Step 3: 编写最小实现**

`solver.go` 增加：

```go
func (s *Solver) SolveWithNonce(challenge []byte, nonce uint64) ([]Solution, error) {
	return s.Solve(appendNonce(challenge, nonce))
}
```

`verifier.go` 增加：

```go
func (v *Verifier) VerifyWithNonce(challenge []byte, nonce uint64, sol Solution) error {
	return v.Verify(appendNonce(challenge, nonce), sol)
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `CGO_ENABLED=1 go test ./ -run 'TestNonceRoundTrip|TestNonceConcatInvariant' -count=1`

Expected: `PASS`

- [ ] **Step 5: Commit**

```bash
git add solver.go verifier.go equix_test.go
git commit -m "Add SolveWithNonce and VerifyWithNonce as challenge||le64(nonce)."
```

---

### Task 5: SolveWithHashes

**Files:**
- Create: `internal/native/hashes.c`
- Create: `internal/native/hashes.go`
- Modify: `internal/native/cgo.go`（若需在 preamble 声明胶水函数）
- Modify: `solver.go`
- Modify: `equix_test.go`

**Interfaces:**
- Consumes: `equix_solve` 之后 ctx 上已 `hashwx_make`；`hashwx_exec`；`Solution`/`Hashes`/`Result`
- Produces:
  - C: `void native_fill_hashes(equix_ctx* ctx, const uint16_t idx[8], uint64_t out[8]);`
  - `func (*native.Context) FillHashes(idx [8]uint16) ([8]uint64, error)`
  - `func (*native.Context) SolveWithHashes(challenge []byte) (indices [][8]uint16, hashes [][8]uint64, err error)`
  - `func (*Solver) SolveWithHashes(challenge []byte) ([]Result, error)`
  - `func (*Solver) SolveWithHashesAndNonce(challenge []byte, nonce uint64) ([]Result, error)`

- [ ] **Step 1: 编写失败的测试**

追加到 `equix_test.go`：

```go
func TestSolveWithHashesMatchesSolve(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	ch, _ := findSolution(t)
	sols, err := s.Solve(ch)
	if err != nil {
		t.Fatal(err)
	}
	results, err := s.SolveWithHashes(ch)
	if err != nil {
		t.Fatal(err)
	}
	if len(results) != len(sols) {
		t.Fatalf("len %d != %d", len(results), len(sols))
	}
	for i := range sols {
		if results[i].Solution != sols[i] {
			t.Fatalf("solution %d", i)
		}
		if err := VerifyHashes(results[i].Hashes); err != nil {
			t.Fatalf("hashes %d: %v", i, err)
		}
		sum := uint64(0)
		for _, h := range results[i].Hashes {
			sum += h
		}
		if sum&((uint64(1)<<60)-1) != 0 {
			t.Fatalf("low 60 bits of sum = %x", sum)
		}
	}
}

func TestSolveWithHashesAndNonce(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	ch := []byte("h")
	var nonce uint64
	var results []Result
	for n := uint64(0); n < 32 && len(results) == 0; n++ {
		nonce = n
		results, err = s.SolveWithHashesAndNonce(ch, nonce)
		if err != nil {
			t.Fatal(err)
		}
	}
	if len(results) == 0 {
		t.Fatal("no solution")
	}
	sols, err := s.SolveWithNonce(ch, nonce)
	if err != nil {
		t.Fatal(err)
	}
	if len(sols) != len(results) {
		t.Fatal("len")
	}
	for i := range sols {
		if sols[i] != results[i].Solution {
			t.Fatalf("sol %d", i)
		}
	}
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `CGO_ENABLED=1 go test ./ -run 'TestSolveWithHashes' -count=1`

Expected: 失败（方法未定义）

- [ ] **Step 3: 编写最小实现**

`internal/native/hashes.c`：

```c
#include <stdint.h>
#include <string.h>
#include <equix.h>
#include <hashwx.h>
#include "context.h"
#include "blake2.h"

extern const blake2b_param equix_v2_blake2_params;

void native_make_hashwx(equix_ctx* ctx, const void* challenge, size_t n) {
	uint8_t seed[HASHWX_SEED_SIZE];
	blake2b_state st;
	hashx_blake2b_init_param(&st, &equix_v2_blake2_params);
	hashx_blake2b_update(&st, challenge, n);
	hashx_blake2b_final(&st, seed, HASHWX_SEED_SIZE);
	hashwx_make(ctx->hash_v2, seed);
}

void native_fill_hashes(equix_ctx* ctx, const uint16_t idx[8], uint64_t out[8]) {
	int i;
	for (i = 0; i < 8; i++) {
		out[i] = hashwx_exec(ctx->hash_v2, idx[i]);
	}
}

void native_make_and_fill_hashes(equix_ctx* ctx, const void* challenge, size_t n,
	const uint16_t idx[8], uint64_t out[8]) {
	native_make_hashwx(ctx, challenge, n);
	native_fill_hashes(ctx, idx, out);
}
```

在 `cgo.go` 的 C preamble 末尾、`import "C"` 之前追加声明：

```c
void native_make_hashwx(equix_ctx* ctx, const void* challenge, size_t n);
void native_fill_hashes(equix_ctx* ctx, const uint16_t idx[8], uint64_t out[8]);
void native_make_and_fill_hashes(equix_ctx* ctx, const void* challenge, size_t n,
	const uint16_t idx[8], uint64_t out[8]);
```

`internal/native/hashes.go`：

```go
package native

func (c *Context) FillHashes(idx [8]uint16) ([8]uint64, error) {
	var zero [8]uint64
	if err := c.dead(); err != nil {
		return zero, err
	}
	var cidx [8]C.uint16_t
	var out [8]C.uint64_t
	for i := 0; i < 8; i++ {
		cidx[i] = C.uint16_t(idx[i])
	}
	C.native_fill_hashes(c.ptr, &cidx[0], &out[0])
	var h [8]uint64
	for i := 0; i < 8; i++ {
		h[i] = uint64(out[i])
	}
	return h, nil
}

func (c *Context) MakeAndFillHashes(challenge []byte, idx [8]uint16) ([8]uint64, error) {
	var zero [8]uint64
	if err := c.dead(); err != nil {
		return zero, err
	}
	var cidx [8]C.uint16_t
	var out [8]C.uint64_t
	for i := 0; i < 8; i++ {
		cidx[i] = C.uint16_t(idx[i])
	}
	ptr, n, free := cChallenge(challenge)
	defer free()
	C.native_make_and_fill_hashes(c.ptr, ptr, n, &cidx[0], &out[0])
	var h [8]uint64
	for i := 0; i < 8; i++ {
		h[i] = uint64(out[i])
	}
	return h, nil
}

func (c *Context) SolveWithHashes(challenge []byte) ([][8]uint16, [][8]uint64, error) {
	sols, err := c.Solve(challenge)
	if err != nil {
		return nil, nil, err
	}
	hs := make([][8]uint64, len(sols))
	for i := range sols {
		hs[i], err = c.FillHashes(sols[i])
		if err != nil {
			return nil, nil, err
		}
	}
	return sols, hs, nil
}
```

`MakeAndFillHashes` 留给 Task 6。不要 import `unsafe`。

`solver.go` 增加：

```go
func (s *Solver) SolveWithHashes(challenge []byte) ([]Result, error) {
	if err := s.closed(); err != nil {
		return nil, err
	}
	idx, hs, err := s.ctx.SolveWithHashes(challenge)
	if err != nil {
		return nil, err
	}
	out := make([]Result, len(idx))
	for i := range idx {
		out[i] = Result{Solution: Solution(idx[i]), Hashes: Hashes(hs[i])}
	}
	return out, nil
}

func (s *Solver) SolveWithHashesAndNonce(challenge []byte, nonce uint64) ([]Result, error) {
	return s.SolveWithHashes(appendNonce(challenge, nonce))
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `CGO_ENABLED=1 go test ./internal/native ./ -run 'TestSolveWithHashes' -count=1`

Expected: `PASS`

- [ ] **Step 5: Commit**

```bash
git add internal/native/hashes.c internal/native/hashes.go internal/native/cgo.go solver.go equix_test.go
git commit -m "Return HashWX values with each Equi-X v2 solution."
```

---

### Task 6: VerifyWithHashes

**Files:**
- Modify: `internal/native/hashes.go`（使用已有 `MakeAndFillHashes`）
- Modify: `verifier.go`
- Modify: `equix_test.go`

**Interfaces:**
- Consumes: `native.MakeAndFillHashes`；`(*native.Context).Verify`
- Produces:
  - `func (*native.Context) VerifyWithHashes(challenge []byte, idx [8]uint16) (hashes [8]uint64, code int, err error)`
  - `func (*Verifier) VerifyWithHashes(challenge []byte, sol Solution) (Hashes, error)`
  - `func (*Verifier) VerifyWithHashesAndNonce(challenge []byte, nonce uint64, sol Solution) (Hashes, error)`

约定：先 `MakeAndFillHashes`，再 `Verify`。校验失败仍返回已算出的哈希。`closed` 时返回零值 `Hashes` 和 `ErrClosed`。

- [ ] **Step 1: 编写失败的测试**

```go
func TestVerifyWithHashesMatchesSolve(t *testing.T) {
	ch, sol := findSolution(t)
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	results, err := s.SolveWithHashes(ch)
	if err != nil {
		t.Fatal(err)
	}
	var want Hashes
	for _, r := range results {
		if r.Solution == sol {
			want = r.Hashes
			break
		}
	}
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	got, err := v.VerifyWithHashes(ch, sol)
	if err != nil {
		t.Fatal(err)
	}
	if got != want {
		t.Fatalf("%v != %v", got, want)
	}
}

func TestVerifyWithHashesStillReturnsOnOrder(t *testing.T) {
	ch, sol := findSolution(t)
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	results, err := s.SolveWithHashes(ch)
	if err != nil {
		t.Fatal(err)
	}
	var want Hashes
	for _, r := range results {
		if r.Solution == sol {
			want = r.Hashes
			break
		}
	}
	bad := sol
	bad[0], bad[1] = bad[1], bad[0]
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	got, err := v.VerifyWithHashes(ch, bad)
	if !errors.Is(err, ErrOrder) {
		t.Fatalf("got %v, want ErrOrder", err)
	}
	if got == (Hashes{}) {
		t.Fatal("hashes must still be populated")
	}
	// swapped indices → swapped first two hash slots vs valid solution
	if got[0] != want[1] || got[1] != want[0] {
		t.Fatalf("hashes should follow the (swapped) indices: got %v want swap of %v", got, want)
	}
}

func TestVerifyWithHashesAndNonce(t *testing.T) {
	s, err := NewSolver()
	if err != nil {
		t.Fatal(err)
	}
	defer s.Close()
	v, err := NewVerifier()
	if err != nil {
		t.Fatal(err)
	}
	defer v.Close()
	ch := []byte("n")
	var nonce uint64
	var results []Result
	for n := uint64(0); n < 32 && len(results) == 0; n++ {
		nonce = n
		results, err = s.SolveWithHashesAndNonce(ch, nonce)
		if err != nil {
			t.Fatal(err)
		}
	}
	if len(results) == 0 {
		t.Fatal("no solution")
	}
	h, err := v.VerifyWithHashesAndNonce(ch, nonce, results[0].Solution)
	if err != nil {
		t.Fatal(err)
	}
	if h != results[0].Hashes {
		t.Fatal("hash mismatch")
	}
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `CGO_ENABLED=1 go test ./ -run 'TestVerifyWithHashes' -count=1`

Expected: 失败（方法未定义）

- [ ] **Step 3: 编写最小实现**

`internal/native/hashes.go` 增加：

```go
func (c *Context) VerifyWithHashes(challenge []byte, idx [8]uint16) ([8]uint64, int, error) {
	var zero [8]uint64
	h, err := c.MakeAndFillHashes(challenge, idx)
	if err != nil {
		return zero, -1, err
	}
	code, err := c.Verify(challenge, idx)
	if err != nil {
		return h, -1, err
	}
	return h, code, nil
}
```

`verifier.go` 增加：

```go
func (v *Verifier) VerifyWithHashes(challenge []byte, sol Solution) (Hashes, error) {
	var zero Hashes
	if err := v.closed(); err != nil {
		return zero, err
	}
	h, code, err := v.ctx.VerifyWithHashes(challenge, [8]uint16(sol))
	if err != nil {
		return zero, err
	}
	return Hashes(h), mapVerify(code)
}
```

`VerifyWithHashesAndNonce`：

```go
func (v *Verifier) VerifyWithHashesAndNonce(challenge []byte, nonce uint64, sol Solution) (Hashes, error) {
	return v.VerifyWithHashes(appendNonce(challenge, nonce), sol)
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `CGO_ENABLED=1 go test ./ -run 'TestVerifyWithHashes' -count=1`

Expected: `PASS`

- [ ] **Step 5: Commit**

```bash
git add internal/native/hashes.go verifier.go equix_test.go
git commit -m "Add VerifyWithHashes that returns HashWX values even on check failure."
```

---

### Task 7: 包级函数与 sync.Pool

**Files:**
- Create: `equix.go`
- Modify: `equix_test.go`

**Interfaces:**
- Consumes: `NewSolver` `NewVerifier` 及各方法
- Produces:
  - `func Solve(challenge []byte) ([]Solution, error)`
  - `func Verify(challenge []byte, sol Solution) error`
  - `func SolveWithNonce(challenge []byte, nonce uint64) ([]Solution, error)`
  - `func VerifyWithNonce(challenge []byte, nonce uint64, sol Solution) error`
  - `func SolveWithHashes(challenge []byte) ([]Result, error)`
  - `func SolveWithHashesAndNonce(challenge []byte, nonce uint64) ([]Result, error)`
  - `func VerifyWithHashes(challenge []byte, sol Solution) (Hashes, error)`
  - `func VerifyWithHashesAndNonce(challenge []byte, nonce uint64, sol Solution) (Hashes, error)`

两个 `sync.Pool`。`New` 在分配失败时返回 `nil`（不缓存错误）。Get 到 `nil` 则再调 `NewSolver`/`NewVerifier` 并把错误交给调用方。Put 回去的必须是未 Close 的实例。包级路径不要 `Close` 池中对象。

- [ ] **Step 1: 编写失败的测试**

```go
func TestPackageLevelSolveVerify(t *testing.T) {
	ch, sol := findSolution(t)
	sols, err := Solve(ch)
	if err != nil {
		t.Fatal(err)
	}
	if len(sols) == 0 {
		t.Fatal("expected solutions")
	}
	if err := Verify(ch, sol); err != nil {
		t.Fatal(err)
	}
	if _, err := SolveWithNonce([]byte("p"), 0); err != nil {
		t.Fatal(err)
	}
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `CGO_ENABLED=1 go test ./ -run TestPackageLevelSolveVerify -count=1`

Expected: 失败（包级 `Solve` 未定义）

- [ ] **Step 3: 编写最小实现**

`equix.go`：

```go
package equix

import "sync"

var (
	solverPool = sync.Pool{New: func() any {
		s, err := NewSolver()
		if err != nil {
			return nil
		}
		return s
	}}
	verifierPool = sync.Pool{New: func() any {
		v, err := NewVerifier()
		if err != nil {
			return nil
		}
		return v
	}}
)

func getSolver() (*Solver, error) {
	if v := solverPool.Get(); v != nil {
		return v.(*Solver), nil
	}
	return NewSolver()
}

func getVerifier() (*Verifier, error) {
	if v := verifierPool.Get(); v != nil {
		return v.(*Verifier), nil
	}
	return NewVerifier()
}

func Solve(challenge []byte) ([]Solution, error) {
	s, err := getSolver()
	if err != nil {
		return nil, err
	}
	defer solverPool.Put(s)
	return s.Solve(challenge)
}

func Verify(challenge []byte, sol Solution) error {
	v, err := getVerifier()
	if err != nil {
		return err
	}
	defer verifierPool.Put(v)
	return v.Verify(challenge, sol)
}

func SolveWithNonce(challenge []byte, nonce uint64) ([]Solution, error) {
	s, err := getSolver()
	if err != nil {
		return nil, err
	}
	defer solverPool.Put(s)
	return s.SolveWithNonce(challenge, nonce)
}

func VerifyWithNonce(challenge []byte, nonce uint64, sol Solution) error {
	v, err := getVerifier()
	if err != nil {
		return err
	}
	defer verifierPool.Put(v)
	return v.VerifyWithNonce(challenge, nonce, sol)
}

func SolveWithHashes(challenge []byte) ([]Result, error) {
	s, err := getSolver()
	if err != nil {
		return nil, err
	}
	defer solverPool.Put(s)
	return s.SolveWithHashes(challenge)
}

func SolveWithHashesAndNonce(challenge []byte, nonce uint64) ([]Result, error) {
	s, err := getSolver()
	if err != nil {
		return nil, err
	}
	defer solverPool.Put(s)
	return s.SolveWithHashesAndNonce(challenge, nonce)
}

func VerifyWithHashes(challenge []byte, sol Solution) (Hashes, error) {
	v, err := getVerifier()
	if err != nil {
		return Hashes{}, err
	}
	defer verifierPool.Put(v)
	return v.VerifyWithHashes(challenge, sol)
}

func VerifyWithHashesAndNonce(challenge []byte, nonce uint64, sol Solution) (Hashes, error) {
	v, err := getVerifier()
	if err != nil {
		return Hashes{}, err
	}
	defer verifierPool.Put(v)
	return v.VerifyWithHashesAndNonce(challenge, nonce, sol)
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `CGO_ENABLED=1 go test ./ -run TestPackageLevelSolveVerify -count=1`

Expected: `PASS`

- [ ] **Step 5: Commit**

```bash
git add equix.go equix_test.go
git commit -m "Add pooled package-level Solve and Verify helpers."
```

---

### Task 8: 官方 tests.c 路径、排列测试、README、全量测试

**Files:**
- Modify: `equix_test.go`
- Modify: `README.md`

**Interfaces:**
- Consumes: 此前全部公开 API
- Produces: 覆盖 spec「测试」第 1、6、8 条；README 含用法、cgo 要求、上游 SHA、MIT+LGPL 说明

- [ ] **Step 1: 编写失败的测试**

追加（与官方 `tests.c` 的 v2 段对齐：交换 `[0]` 与 `[4..7]` 整组 → `ErrOrder`；8! 排列恰好一个合法）：

```go
func TestVerifyTreeSwapOrder(t *testing.T) {
	ch, sol := findSolution(t)
	swapped := sol
	for i := 0; i < 4; i++ {
		swapped[i], swapped[i+4] = swapped[i+4], swapped[i]
	}
	if err := Verify(ch, swapped); !errors.Is(err, ErrOrder) {
		t.Fatalf("got %v, want ErrOrder", err)
	}
}

func TestOnlyOnePermutationValid(t *testing.T) {
	if testing.Short() {
		t.Skip("40320 verifies")
	}
	ch, sol := findSolution(t)
	idx := sol
	valid := 0
	var permute func(int)
	permute = func(start int) {
		if start == 7 {
			if Verify(ch, Solution(idx)) == nil {
				valid++
			}
			return
		}
		for i := start; i < 8; i++ {
			idx[start], idx[i] = idx[i], idx[start]
			permute(start + 1)
			idx[start], idx[i] = idx[i], idx[start]
		}
	}
	permute(0)
	if valid != 1 {
		t.Fatalf("valid permutations=%d, want 1", valid)
	}
}

func BenchmarkSolve(b *testing.B) {
	s, err := NewSolver()
	if err != nil {
		b.Fatal(err)
	}
	defer s.Close()
	ch := []byte("bench")
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		if _, err := s.Solve(ch); err != nil {
			b.Fatal(err)
		}
	}
}

func BenchmarkVerify(b *testing.B) {
	ch, sol := func() ([]byte, Solution) {
		s, err := NewSolver()
		if err != nil {
			b.Fatal(err)
		}
		defer s.Close()
		for n := 0; n < 32; n++ {
			c := []byte{byte(n)}
			sols, err := s.Solve(c)
			if err != nil {
				b.Fatal(err)
			}
			if len(sols) > 0 {
				return c, sols[0]
			}
		}
		b.Fatal("no solution")
		return nil, Solution{}
	}()
	v, err := NewVerifier()
	if err != nil {
		b.Fatal(err)
	}
	defer v.Close()
	b.ResetTimer()
	for i := 0; i < b.N; i++ {
		if err := v.Verify(ch, sol); err != nil {
			b.Fatal(err)
		}
	}
}
```

- [ ] **Step 2: 运行测试确认新用例能编译**

Run: `CGO_ENABLED=1 go test ./ -run 'TestVerifyTreeSwapOrder|TestOnlyOnePermutationValid' -count=1`

Expected: `PASS`（不依赖新生产代码。`TestOnlyOnePermutationValid` 较慢。）

- [ ] **Step 3: 更新 README**

将 `README.md` 写成如下内容（Go 示例用缩进代码块，避免围栏嵌套）：

````markdown
# equix-cgo

Go cgo 封装，绑定 [tevador/equix](https://github.com/tevador/equix) 的 **equix_v2** 分支（HashWX）。

    import "github.com/cxio/equix-cgo"

    sols, err := equix.SolveWithNonce(challenge, nonce)
    if err != nil {
        return err
    }
    for _, sol := range sols {
        h, err := equix.VerifyWithHashesAndNonce(challenge, nonce, sol)
        if err != nil {
            return err
        }
        _ = h
    }

需要 `CGO_ENABLED=1` 和 C 编译器（gcc/clang；Windows 用 MinGW）。不支持 `CGO_ENABLED=0`。

钉死的上游 commit：

- equix `350a85dedda1344637dac09a1de786ee63a5fb01`（`equix_v2`）
- hashx `08babdf4f41b0b8991d1fa94914c7c6902de0cb6`
- hashwx `d771cbf6cdc070755f7d137cdcf9d781af14da3f`

## 许可

本仓库 Go 代码为 MIT。`third_party/equix`、`third_party/hashx`、`third_party/hashwx` 为 LGPL-3.0。静态链入官方 C 构成 LGPL 组合作品；分发二进制时须保留 LGPL 声明，并提供这些 C 源码（本仓库已包含）。
````

- [ ] **Step 4: 运行全量测试**

Run: `CGO_ENABLED=1 go test ./... -count=1`

Expected: `PASS`。`TestOnlyOnePermutationValid` 可能要数十秒；可用 `go test ./... -count=1 -short` 作快速路径，全量再跑一次不带 `-short`。

基准不作为失败条件：`go test ./ -bench . -benchtime=1x` 仅作冒烟。

- [ ] **Step 5: Commit**

```bash
git add equix_test.go README.md
git commit -m "Cover Wagner order checks and document cgo plus LGPL pins."
```

---

## 自审

**Spec 覆盖**

| Spec 需求 | 任务 |
|-----------|------|
| Solve/Verify、Solver/Verifier | 3、7 |
| challenge \|\| le64(nonce) | 1（`appendNonce`）、4、7 |
| SolveWithHashes* | 5、7 |
| VerifyWithHashes*（失败也返回哈希；先 make） | 6、7 |
| VerifyHashes 纯 Go 15/30/60 位 | 1 |
| 钉死 commit、不改 third_party、stub 编译 | 2 |
| COMPILE 回退、无大页、EQUIX_V2 | 2 |
| 0 解空切片、非法 count、ErrClosed、Close 幂等 | 3 |
| sync.Pool | 7 |
| 官方 tests.c v2 / 排列 / nonce / 拼接 / 哈希一致性 | 3–6、8 |
| README 许可与 pin | 8 |
| 不导出 HashX/COMPILE/v1 | 全程未导出 |

**类型一致性：** `Solution [8]uint16`、`Hashes [8]uint64`、`Result`、native `[8]uint16`/`[8]uint64` 切片在各任务间名称不变。`mapVerify` 的 0–4 与 C `equix_result` 顺序一致。
